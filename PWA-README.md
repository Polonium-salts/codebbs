# PWA 功能说明

本项目已集成PWA（Progressive Web App）功能，使用户可以将网站安装到设备上，并提供离线访问能力。

## 功能特点

- **可安装性**：用户可以将应用安装到设备主屏幕
- **离线工作**：即使在没有网络连接的情况下也能访问应用的基本功能
- **响应式设计**：适配各种设备尺寸
- **推送通知**：（未来可扩展功能）

## 技术实现

PWA功能通过以下技术实现：

1. **Service Worker**：用于缓存资源和提供离线功能
2. **Manifest文件**：定义应用的元数据和安装行为
3. **安装按钮**：提供用户友好的安装体验

## 如何生成PWA图标

项目需要各种尺寸的图标来支持不同设备。按照以下步骤生成图标：

1. 准备一个高分辨率的方形图标（建议至少512x512像素），命名为`source-icon.png`并放在项目根目录
2. 安装依赖：`npm install sharp --save-dev`
3. 运行图标生成脚本：`npm run generate-pwa-icons`

这将在`public/icons`目录下生成所有必要的图标。

## 测试PWA功能

要测试PWA功能，请按照以下步骤操作：

1. 构建生产版本：`npm run build`
2. 启动生产服务器：`npm start`
3. 使用Chrome或Edge浏览器访问应用
4. 在地址栏右侧查找安装图标，或使用应用中的"安装应用"按钮

## iOS设备上的安装

在iOS设备上，PWA的安装过程略有不同：

1. 使用Safari浏览器访问应用
2. 点击分享按钮
3. 选择"添加到主屏幕"选项

## 注意事项

- PWA功能在生产环境中效果最佳
- 某些功能可能需要HTTPS才能正常工作
- 定期更新`sw.js`中的`CACHE_NAME`以确保用户获取最新版本的应用

## 故障排除

如果遇到PWA相关问题：

1. 确保所有PWA图标都已正确生成
2. 验证`manifest.json`文件是否有效
3. 检查Service Worker是否正确注册
4. 使用Chrome开发者工具中的"Application"标签页调试PWA问题 